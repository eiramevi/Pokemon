<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/header") %>
  </head>
  <body class="bg-yellow-500 from-green-300 to-blue-500 flex flex-col items-center justify-center h-screen">
    <div class="bg-red-700 text-white rounded-xl p-6 shadow-lg w-[400px] h-[550px] relative flex flex-col items-center">
      <!-- Top UI Indicators -->
      <div class="absolute top-3 left-4 flex space-x-2">
        <div class="w-5 h-5 bg-blue-500 rounded-full border border-white"></div>
        <div class="w-4 h-4 bg-yellow-500 rounded-full border border-white"></div>
        <div class="w-3 h-3 bg-green-500 rounded-full border border-white"></div>
      </div>
      
      <!-- Screen Section -->
      <div class="bg-gray-100 p-3 rounded-md flex items-center justify-center w-56 h-40 mt-8 border-4 border-black shadow-inner">
        <img src="<%= pokemon.sprites.front_default %>" alt="<%= pokemon.name %>" class="w-28 h-28">
      </div>
      
      <!-- Pokemon Info -->
       <div class="bg-white text-black rounded-md p-3 mt-2 w-52 text-center border border-gray-300">
        <h2 class="text-xl font-bold text-center mt-4"><%= pokemon.name %></h2>
       </div>
      
      <div class="bg-white text-black rounded-md p-3 mt-2 w-52 text-center border border-gray-300">
        <p class="text-sm font-semibold">Height: <%= pokemon.height %></p>
        <p class="text-sm font-semibold">Weight: <%= pokemon.weight %></p>
        <p class="text-sm font-semibold">Type: <%= pokemon.types.map(t => t.type.name).join(', ') %></p>
      </div>
      
      <!-- Buttons Section -->
      <div class=" gap-2 mt-8 p-3">
        <a href="/" class="bg-blue-500 text-white px-4 py-2 rounded-lg col-span-3 text-center">Back</a>
      </div>
    </div>
    
    <%- include("../partials/footer") %>
  </body>
</html>
